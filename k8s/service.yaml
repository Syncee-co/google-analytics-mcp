apiVersion: v1
kind: Service
metadata:
  name: mcp-service
  namespace: default
  annotations:
    cloud.google.com/neg: '{"ingress": true}'
    cloud.google.com/app-protocols: '{"http-port":"HTTP"}'
spec:
  selector:
    app: mcp-ga
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: NodePort
